import { SdfScene } from './scene.js';
import type { ProbeResult } from './pack.js';
import { type SdfKeyframeConfig } from './keyframe-bridge.js';
export interface SdfWebGPUPickResult {
    hit: boolean;
    source: string;
    point: [number, number, number];
    normal: [number, number, number];
    distance: number;
    unit: string;
    label: string;
    labelA: number;
    labelB: number;
    blend: number;
    curvature: number;
    extra?: Record<string, any>;
}
export type UnifiedPick = SdfWebGPUPickResult;
export declare class SdfWebGPUEngine {
    readonly backend = "WebGPU / WGSL";
    adapterName: string;
    floatProbe: boolean;
    private canvas;
    private device;
    private ctx;
    private pipeline;
    private probePipeline;
    private ubuf;
    private bind;
    private probeBind;
    private probeTex;
    private probeBuf;
    private ready;
    onError?: (msg: string) => void;
    sc: SdfScene;
    private st;
    private P1;
    private P3;
    private waveMax;
    private wearAges;
    private wearKey;
    wearAge: number;
    yaw: number;
    pitch: number;
    dist: number;
    zoom: number;
    timeSec: number;
    paused: boolean;
    speed: number;
    renderScale: number;
    fps: number;
    private fpsEma;
    private frames;
    private lastT;
    private animId;
    assemble: boolean;
    pulse: boolean;
    orbit: boolean;
    clusterOn: boolean;
    clusterCell: number;
    clusterSpread: number;
    private radius0;
    private constructor();
    static create(canvas: HTMLCanvasElement, onError?: (msg: string) => void): Promise<SdfWebGPUEngine>;
    private static inflight;
    private static createInner;
    setScene(idx: number): void;
    loadScene(raw: SdfScene): void;
    setKeyframeConfig(config: SdfKeyframeConfig): Promise<boolean>;
    enableKeyframeEngine(config: SdfKeyframeConfig): Promise<boolean>;
    disableKeyframeBridge(): void;
    disableKeyframeEngine(): void;
    get isKeyframeActive(): boolean;
    start(): void;
    stop(): void;
    private camQ;
    private draw;
    frame(nowMs: number): void;
    probe(ndcX: number, ndcY: number): Promise<ProbeResult | null>;
    pickAt(clientX: number, clientY: number): Promise<UnifiedPick | null>;
    resetView(): void;
    dispose(): void;
}
